# Weekly ops report: 2026-08-12

Fourth scheduled ops run, T0+5 (launch 2026-08-07).

## 1. Health check

All routes reachable and returning `200 OK`.

| Route | Result |
|---|---|
| `/` | 200 |
| `/lab` | 200 |
| `/methodology` | 200 |
| `/rss.xml` | 200 |
| `/autopsies/congress-mirror` | 200 |
| `/autopsies/round-numbers` | 200 |
| `/autopsies/buying-options` | 200 |
| homepage contains `cloudflareinsights` | present |
| homepage contains `embed-subscribe/morguelabs` | present |

(`/autopsies/nba-fatigue` is new this run, see §3, and is not yet live on
`morguelabs.com`, since it ships with this PR and publishes only once the
operator merges. It is not part of this run's pre-change health check.)

## 2. Metrics

**Traffic** (Cloudflare GraphQL, `rumPageloadEventsAdaptiveGroups`,
`siteTag` = configured `CF_SITE_TAG`): as in prior weeks, the dataset does
not expose a distinct "unique visitors" field, only `sum.visits`
(Cloudflare's session-based visit count, the closest available proxy) and
`count` (raw pageload events). Reporting `visits` below with that caveat;
treat it as a lower-bound proxy for uniques, not an exact match to the
charter's "unique visitors" wording.

- Visits, trailing 7 days (2026-08-05 to 2026-08-12): **52**
- Visits, cumulative since launch (2026-08-07 to 2026-08-12): **52**
  (the two windows still coincide, the site is 5 days old, so the
  trailing-7-day window fully contains the site's lifetime)
- Top pages, trailing 7 days:
  - `/`: 45 visits
  - `/autopsies/congress-mirror`: 3 visits
  - `/methodology`: 1 visit
  - `/lab`: 1 visit
  - `/autopsies/buying-options`: 1 visit
  - `/autopsies/round-numbers`: 1 visit

**Subscribers** (Buttondown `/v1/subscribers`, `count` field): **0** active
subscribers.

## 3. Cadence

Newest `pubDate` in `rss.xml` at the start of this run: Thu, 06 Aug 2026
17:00:00 GMT (Autopsy №003, "Buying options for the big win"). That is 6
days before today. The cadence promise tightened 2026-08-11 to at least one
new autopsy per week, so 6 days trips the "due" threshold. An autopsy is
drafted this run.

Drafted **Autopsy №004: NBA back-to-back fatigue betting** (`nba-fatigue`),
the first freezer entry not already published, in freezer order. All
numbers in the piece are copied verbatim or as trivial unit restatements
(decimal to percent) from the freezer note; no figures were invented. Word
count 787 (excluding toe tag and boilerplate), matching house style: toe-tag
block, five standard sections, dry clinical tone, zero em dashes (plain
hyphens used throughout for the sample date range and for edge/percentage
figures).

Changes in this PR:
- `autopsies/nba-fatigue.html`: new autopsy page
- `rss.xml`: new item, link/guid `https://morguelabs.com/autopsies/nba-fatigue`,
  no `.html` extension, pubDate Wed, 12 Aug 2026 17:00:00 GMT
- `index.html`: new case-file card for №004; removed the matching
  "On the slab" bullet

**Autopsies live once merged (4):**
- №001: Copying Congress's stock trades (`congress-mirror`)
- №002: Round-number support and resistance (`round-numbers`)
- №003: Buying options for the big win (`buying-options`)
- №004: NBA back-to-back fatigue betting (`nba-fatigue`), pending merge

**Freezer remaining (2, in publish order):**
- Post-earnings drift in large caps (`earnings-drift`)
- Crypto momentum rotation (`crypto-momentum`)

At the current weekly cadence, the freezer runs out in 2 more weekly runs.
Flagging early so the desk has lead time: after `earnings-drift` and
`crypto-momentum` publish, the freezer is empty and a run will report
FREEZER EMPTY rather than invent or thin out a piece.

## 4. Charter clock

- Days until interim check (2026-09-06, T0+30): **25**
- Days until main gate (2026-11-05, T0+90): **85**

**Distance to interim criterion** (cumulative uniques < 200 at T0+30 leads
to pivot allowed): 52 visits so far against the <200 threshold. Still
early; 5 days of data.

**Distance to main gate criteria** (any one of the three clears it):
- Email subscribers: **0 / 100**
- Best single piece, 7-day uniques: **45 / 1,000** (top page this week,
  `/`, using visits as the uniques proxy noted above; the charter
  criterion is meant for a single autopsy piece, and the leading autopsy
  page, `/autopsies/congress-mirror`, sits at 3 / 1,000)
- Cumulative uniques: **52 / 3,000** (visits proxy)

No distribution attempt is observable this run: no referrer or campaign
data appeared in the top-pages breakdown, and outbound posting is outside
this agent's remit under the standing rules (approved channels are curated
aggregators and press pitches, both human-run).

## 5. Operator asks

- PR for this run (draft + report) is open and awaiting your merge; merging
  is the publication approval for both the new autopsy and this report.
- PR #10 ("Retract the significance claim; add the holding-period section")
  is open on `fix/clustered-t-retraction-and-horizons` and awaiting your
  review/merge. It is outside this run's scope (not a weekly-ops PR) and
  was left untouched.
- Freezer has 2 entries left (`earnings-drift`, `crypto-momentum`). At
  1/week, that's the last new autopsy in roughly 2 weeks. If the desk has
  more kill records in the pipeline, this is the point to queue them so the
  cadence promise doesn't lapse into a FREEZER EMPTY week.
- No other operator action needed this run: network egress, Cloudflare
  token, and Buttondown token are all working.
