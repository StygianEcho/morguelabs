# Weekly ops report — 2026-08-07

First scheduled ops run, one day after T0 (launch 2026-08-07).

**Update (same-day re-run):** the environment's network egress and the
Cloudflare/Buttondown tokens that blocked the first pass earlier today are
now working. This revision replaces the earlier "unavailable" findings
with real results.

## 1. Health check

All routes reachable and returning `200 OK`.

| Route | Result |
|---|---|
| `/` | 200 |
| `/lab` | 200 |
| `/methodology` | 200 |
| `/rss.xml` | 200 |
| `/autopsies/congress-mirror` | 200 |
| `/autopsies/round-numbers` | 200 |
| `/autopsies/buying-options` | 200 |
| homepage contains `cloudflareinsights` | present |
| homepage contains `embed-subscribe/morguelabs` | present |

## 2. Metrics

**Traffic** (Cloudflare GraphQL, `rumPageloadEventsAdaptiveGroups`,
`siteTag` = configured `CF_SITE_TAG`): the dataset does not expose a
distinct "unique visitors" field — only `sum.visits` (Cloudflare's
session-based visit count, the closest available proxy) and `count`
(raw pageload events). Reporting `visits` below with that caveat; treat
it as a lower-bound proxy for uniques, not an exact match to the
charter's "unique visitors" wording.

- Visits, trailing 7 days (2026-07-31 → 2026-08-07): **2**
- Visits, cumulative since launch (2026-08-07 → 2026-08-07): **2**
- Top pages, trailing 7 days:
  - `/autopsies/congress-mirror` — 1 visit
  - `/` — 1 visit

**Subscribers** (Buttondown `/v1/subscribers`, `count` field): **0**
active subscribers.

## 3. Cadence

Newest `pubDate` in `rss.xml`: Thu, 06 Aug 2026 17:00:00 GMT (Autopsy
№003, "Buying options for the big win"). That is 1 day before today —
well under the 13-day threshold, so no autopsy is due this run. No
draft was produced.

**Autopsies live (3):**
- №001 — Copying Congress's stock trades (`congress-mirror`)
- №002 — Round-number support and resistance (`round-numbers`)
- №003 — Buying options for the big win (`buying-options`)

**Freezer remaining (3, in publish order):**
- NBA back-to-back fatigue betting (`nba-fatigue`)
- Post-earnings drift in large caps (`earnings-drift`)
- Crypto momentum rotation (`crypto-momentum`)

## 4. Charter clock

- Days until interim check (2026-09-06, T0+30): **30**
- Days until main gate (2026-11-05, T0+90): **90**

**Distance to interim criterion** (cumulative uniques < 200 at T0+30 →
pivot allowed): 2 visits so far against the <200 threshold. Far too
early to read anything into this — one day post-launch.

**Distance to main gate criteria** (any one of the three clears it):
- Email subscribers: **0 / 100**
- Best single piece, 7-day uniques: **1 / 1,000** (top page this week,
  `/autopsies/congress-mirror`, using visits as the uniques proxy noted
  above)
- Cumulative uniques: **2 / 3,000** (visits proxy)

## 5. Operator asks

- PR #1 ("Weekly ops 2026-08-07") is open and awaiting your merge —
  merging is the publication approval for this report update.
- No other operator action needed this run: network egress, Cloudflare
  token, and Buttondown token are all working.
