# Weekly ops report — 2026-08-07

First scheduled ops run, one day after T0 (launch 2026-08-07).

## 1. Health check

Could not be performed. The remote execution environment's outbound
network egress policy blocks `morguelabs.com` entirely — every request
(direct `curl` and the sandboxed `WebFetch` tool) was rejected at the
proxy gateway with `403 Forbidden` / `EGRESS_BLOCKED` before reaching the
site. This is an environment/network-policy issue, not a signal about
the site's actual status; no conclusion about uptime can be drawn from
it either way.

| Route | Result |
|---|---|
| `/` | not reachable — egress blocked |
| `/lab` | not reachable — egress blocked |
| `/methodology` | not reachable — egress blocked |
| `/rss.xml` | not reachable — egress blocked |
| `/autopsies/*` (per rss.xml) | not reachable — egress blocked |
| homepage contains `cloudflareinsights` | not checked — egress blocked |

## 2. Metrics

`CF_API_TOKEN` / `CF_ACCOUNT_ID` / `CF_SITE_TAG` are not set in this
environment. Metrics unavailable — operator has not yet provided a
Cloudflare Analytics read token.

## 3. Cadence

Newest `pubDate` in `rss.xml`: Thu, 06 Aug 2026 17:00:00 GMT (Autopsy
№003, "Buying options for the big win"). That is 1 day before today —
well under the 13-day threshold, so no autopsy is due this run. No
draft was produced.

**Autopsies live (3):**
- №001 — Copying Congress's stock trades (`congress-mirror`)
- №002 — Round-number support and resistance (`round-numbers`)
- №003 — Buying options for the big win (`buying-options`)

**Freezer remaining (3, in publish order):**
- NBA back-to-back fatigue betting (`nba-fatigue`)
- Post-earnings drift in large caps (`earnings-drift`)
- Crypto momentum rotation (`crypto-momentum`)

## 4. Charter clock

- Days until interim check (2026-09-06, T0+30): **30**
- Days until main gate (2026-11-05, T0+90): **90**

**Distance to interim criterion** (cumulative uniques < 200 → pivot
allowed): unknown — metrics unavailable this run.

**Distance to main gate criteria** (any one of: ≥100 email subscribers,
one piece with ≥1,000 uniques in a 7-day window, or ≥3,000 cumulative
uniques):
- Email subscribers: site currently reads "email list soon" — no
  subscription mechanism is live yet, so this criterion is presently
  unreachable until one exists.
- 7-day/cumulative unique visitors: unknown — metrics unavailable this
  run.

## 5. Operator asks

- **Network egress for this environment blocks `morguelabs.com`.**
  Automated health checks cannot run until this session (or its
  environment's network policy) is allowed to reach the production
  domain. Needs operator action on the environment configuration.
- **Cloudflare Analytics read token not provided.** Set `CF_API_TOKEN`,
  `CF_ACCOUNT_ID`, and `CF_SITE_TAG` in this environment so weekly
  metrics (uniques, top pages) can be pulled — this is also how
  progress toward both charter gates gets measured going forward.
- No pull requests are currently open awaiting review.
