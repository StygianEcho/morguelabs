# Weekly ops report — 2026-08-10

Second scheduled ops run, T0+3 (launch 2026-08-07).

## 1. Health check

All routes reachable and returning `200 OK`.

| Route | Result |
|---|---|
| `/` | 200 |
| `/lab` | 200 |
| `/methodology` | 200 |
| `/rss.xml` | 200 |
| `/autopsies/congress-mirror` | 200 |
| `/autopsies/round-numbers` | 200 |
| `/autopsies/buying-options` | 200 |
| homepage contains `cloudflareinsights` | present |
| homepage contains `embed-subscribe/morguelabs` | present |

## 2. Metrics

**Traffic** (Cloudflare GraphQL, `rumPageloadEventsAdaptiveGroups`,
`siteTag` = configured `CF_SITE_TAG`): as noted last week, the dataset
does not expose a distinct "unique visitors" field — only `sum.visits`
(Cloudflare's session-based visit count, the closest available proxy)
and `count` (raw pageload events). Reporting `visits` below with that
caveat; treat it as a lower-bound proxy for uniques, not an exact match
to the charter's "unique visitors" wording.

- Visits, trailing 7 days (2026-08-03 → 2026-08-10): **47**
- Visits, cumulative since launch (2026-08-07 → 2026-08-10): **47**
  (the two windows coincide — the site is only 3 days old, so the
  trailing-7-day window fully contains the site's lifetime)
- Top pages, trailing 7 days:
  - `/` — 41 visits
  - `/autopsies/congress-mirror` — 2 visits
  - `/methodology` — 1 visit
  - `/lab` — 1 visit
  - `/autopsies/buying-options` — 1 visit
  - `/autopsies/round-numbers` — 1 visit

**Subscribers** (Buttondown `/v1/subscribers`, `count` field): **0**
active subscribers.

## 3. Cadence

Newest `pubDate` in `rss.xml`: Thu, 06 Aug 2026 17:00:00 GMT (Autopsy
№003, "Buying options for the big win"). That is 4 days before today —
well under the 13-day threshold, so no autopsy is due this run. No
draft was produced.

**Autopsies live (3):**
- №001 — Copying Congress's stock trades (`congress-mirror`)
- №002 — Round-number support and resistance (`round-numbers`)
- №003 — Buying options for the big win (`buying-options`)

**Freezer remaining (3, in publish order):**
- NBA back-to-back fatigue betting (`nba-fatigue`)
- Post-earnings drift in large caps (`earnings-drift`)
- Crypto momentum rotation (`crypto-momentum`)

## 4. Charter clock

- Days until interim check (2026-09-06, T0+30): **27**
- Days until main gate (2026-11-05, T0+90): **87**

**Distance to interim criterion** (cumulative uniques < 200 at T0+30 →
pivot allowed): 47 visits so far against the <200 threshold. Still
early — under a week of data.

**Distance to main gate criteria** (any one of the three clears it):
- Email subscribers: **0 / 100**
- Best single piece, 7-day uniques: **41 / 1,000** (top page this
  week, `/`, using visits as the uniques proxy noted above — the
  charter criterion is meant for a single autopsy piece; the leading
  autopsy page, `/autopsies/congress-mirror`, sits at 2 / 1,000)
- Cumulative uniques: **47 / 3,000** (visits proxy)

## 5. Operator asks

- PR for this report is open and awaiting your merge — merging is the
  publication approval for this report update.
- PR #6 ("Data provenance: House Clerk cross-check, sample framing") is
  open on `fix/data-provenance` and awaiting your review/merge — it is
  outside this run's scope (not a weekly-ops PR) and was left untouched.
- No other operator action needed this run: network egress, Cloudflare
  token, and Buttondown token are all working.
